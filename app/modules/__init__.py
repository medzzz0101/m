"""Module catalog. Every *.py file here that defines a BaseModule subclass is
auto-discovered by core.registry at startup. Files prefixed with "_" are
helpers and are skipped by the discovery walk."""
